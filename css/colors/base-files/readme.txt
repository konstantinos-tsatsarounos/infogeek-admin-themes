For you to compile a new css, must have already installed on your system, Ruby and SASS! 

Just open the Terminal or CMD within forder theme, and run:

sass -t -f colors.scss colors.css
sass -t compressed -f colors.scss colors.min.css